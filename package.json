{
  "name": "connector",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.ts",
  "scripts": {
    "clear": "rm -r build | rm -r dist | rm settings.json",
    "setup": "ts-node src/install.ts",
    "dev": "nodemon --watch src/**/*.ts -L src/server.ts",
    "build": "rm -r build | rm -r dist | tsc && pkg build/server.js --target node16-win --out-dir dist && pkg build/install.js  --target node16-win --out-dir dist && rm -r build",
    "start": "rm -r build | tsc && node build/server.js"
  },
  "keywords": [],
  "author": "NUB31",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "download": "^8.0.0",
    "express": "^4.18.2",
    "fs-extra": "^10.1.0",
    "socket.io": "^4.5.3"
  },
  "devDependencies": {
    "@types/download": "^8.0.1",
    "@types/express": "^4.17.14",
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^18.11.3",
    "nodemon": "^2.0.20",
    "pkg": "^5.8.0",
    "typescript": "^4.8.4"
  }
}
